{{-- علامةُ حالةٍ تُقرأ لا خانةٌ معطَّلة.

     ═══ ما تُصلحه ═══

     الخانةُ المعطَّلة يرسمها المتصفّح رماديةً مهما كانت مؤشَّرة، فيبدو
     النوعُ المشغَّلُ مطفأً — «لونهم يوحي أنّهم غير فعّالين». والقفلُ
     معنىً آخرُ غير الإطفاء، فلا يُقالان بلونٍ واحد.

     فالعلامةُ هنا تقول الحالةَ وحدها: ذهبيّةٌ فيها صحٌّ إن كان مشغَّلاً،
     وحلقةٌ فارغة إن لم يكن. والقفلُ يُقال بجملةٍ تحت القسم.

     ‏$on   الحالة.  $top  محاذاةٌ لأعلى السطر (بطاقةٌ بسطرين). --}}
<span class="shrink-0 w-4 h-4 rounded flex items-center justify-center text-[10px] leading-none
             {{ ($top ?? true) ? 'mt-0.5' : '' }}
             {{ $on ? 'bg-gold text-white' : 'border border-gray-300' }}"
      role="img"
      aria-label="{{ $on ? 'مشغَّل' : 'مطفأ' }}">{{ $on ? '✓' : '' }}</span>
